apiVersion: apps/v1
kind: Deployment
metadata:
  name: order-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: order-service
  template:
    metadata:
      labels:
        app: order-service
    spec:
      containers:
      - name: order-service
        image: aiopstrainacr.azurecr.io/order-service:v1
        ports:
        - containerPort: 8006
        env:
        - name: COSMOS_ENDPOINT
          valueFrom:
            secretKeyRef:
              name: app-secrets
              key: COSMOS_DB_ENDPOINT
        - name: COSMOS_KEY
          valueFrom:
            secretKeyRef:
              name: app-secrets
              key: COSMOS_DB_KEY
        - name: SERVICE_BUS_CONNECTION_STRING
          valueFrom:
            secretKeyRef:
              name: app-secrets
              key: SERVICE_BUS_CONNECTION_STRING
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "250m"
            memory: "256Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: order-service
spec:
  selector:
    app: order-service
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8006
  type: ClusterIP
